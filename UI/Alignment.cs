namespace UI;

public enum Alignment
{
    // Meant for only VerticalAlignment (treated as Left, Right respectively for HorizontalAlignment)
    Top,
    Bottom,
    // Meant for only HorizontalAlignment (treated as Top, Bottom respectively for VerticalAlignment)
    Left,
    Right,
    // Both
    Center,
}